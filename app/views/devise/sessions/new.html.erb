<div class="jumbotron jumbotron-fluid">
  <div class="container"><h1>Iniciar Sesión</h1>
    <p class="lead">Para poder registrar tus eventos</p>
    <%= link_to  "Regresar", root_path %>
 </div>
</div>

<div class="container">
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :Dirección_de_email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email",  class: "col-md-4 form-control" %>
  </div>

  <div class="field">
    <%= f.label :Contraseña %><br />
    <%= f.password_field :password, autocomplete: "current-password",  class: "col-md-4 form-control" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :recordarme %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "Iniciar Sesión", :class => "col-md-4 btn btn-primary" %>
    <br>
    <%= render "devise/shared/links" %>
  </div>
<% end %>
</div>


